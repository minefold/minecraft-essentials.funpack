name: Minecraft Essentials
description: >
  Essentials
  WorldEdit
  WorldGuard

version: 1.0

settings:
  - name: new_player_can_build
    type: bool
    default: true
    label: New players can build
    hint: New players are able to place and destroy blocks

addons:
  backup:
    ignore: |
      craftbukkit.jar
      server.log
      server.properties